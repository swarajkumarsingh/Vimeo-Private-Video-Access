name: vimeo_demo
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  # Vimeo video Player
  pod_player: ^0.1.0

  # FOr GET request
  http: ^0.13.5

  # For WebView
  webview_flutter: ^3.0.4

  # For cookie accessing 
  webview_cookie_manager: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
